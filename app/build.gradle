apply plugin: 'idea'
apply plugin: 'java'

version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile group: "com.basho.riak", name: "riak-client", version: "${riakVersion}"
}
